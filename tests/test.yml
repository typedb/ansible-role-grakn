---
- hosts: all

  vars:
    grakn_service_state: "stopped"
    grakn_version: "0.16.0"
    grakn_download_url: "https://oss.sonatype.org/service/local/repositories/releases/content/ai/grakn/grakn-dist/{{ grakn_version }}/grakn-dist-{{ grakn_version }}.tar.gz"

  pre_tasks:
    - name: Update apt cache.
      apt: update_cache=yes cache_valid_time=600
      when: ansible_os_family == 'Debian'

  roles:
    - role_under_test
